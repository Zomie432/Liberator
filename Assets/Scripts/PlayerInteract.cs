using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerInteract : MonoBehaviour
{
    public void ProcessInteraction()
    {
        //raycast forward from the player's view

        //check the tag of the game object it collides with if it does collide

        //if door then access door controller script on that object and call door.Interact() which will decide whether to open or close

    }
}
